type User @model
  @auth(rules: [{ allow: owner }]) {
  id: ID!
  name: String!
  letter: [Letter] @hasMany
}

type Letter
  @model
  @auth(rules: [{ allow: owner }]) {
  id: ID!
  name: String!
  userId: ID!
}